---
import { Image } from 'astro:assets';
import FormattedDate from '../components/FormattedDate.astro';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
).slice(0,3);
---
<style>
	#recent-posts-intro ul {
	list-style: none;
	padding: 0.5rem;
	margin: 0.5rem;
	display: flex;
	flex-direction: row;
	align-items: stretch; 
	justify-content: center;
	gap: 1rem;
}
	
	#recent-posts-intro h2 {
	text-align: center;

}

#recent-posts-intro li {
	text-align: center;
	border: 1px solid rgb(var(--gray-light));
	border-radius: 8px;
	padding: 1rem;
	width: 20%;
}

#recent-posts-intro a {
	text-decoration: none;
	color: inherit;
	display: block;
}
#recent-posts-intro .title {
	font-size: 1.4rem;
}

#recent-posts-intro .date {
	font-size: 0.9rem;
	color: rgb(var(--gray));
}

</style>

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>Hey there.</h1>
			<p>
				This is a space I would like to dedicate to my tech adventures, endeavours, deep dive into new domains and so on! <code>divyesh.is-cool.dev</code> will contain devlogs, writing about new discoveries and explorations into the fields of embedded hardware, Linux development, DIY tech as a full stack developer (student currently).
			</p>
			<p>
				You can visit the <code>about</code> page for more information regarding myself and a deeper dive into my journey, as well as ways to contact me for feedback and discrepancies!
			</p>
			
		</main>
		
		<section id="recent-posts-intro">
			<h2>Recent Posts</h2>
				<ul>
					{ posts.length == 3 ? posts.map((post) => (
							<li>
								<a href={`/blog/${post.id}/`}>
									{post.data.heroImage && (
										<Image width={720} height={360} src={post.data.heroImage} alt={post.data.title} loading={'eager'} />
									)}
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.pubDate} />
									</p>
								</a>
							</li>
						)) : 'No Posts Available.'
					}
				</ul>
			</section>
		<Footer />
	</body>
</html>

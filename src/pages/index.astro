---
import { Image } from 'astro:assets';
import FormattedDate from '../components/FormattedDate.astro';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import BaseLayout from '../layouts/BaseLayout.astro';
const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
).slice(0,3);
---
<style>
	#recent-posts-intro {
		margin: 4rem 0;
		padding: 0;
	}

	#recent-posts-intro h2 {
		text-align: center;
		margin-bottom: 2rem;
		font-size: 1.8rem;
	}

	#recent-posts-intro ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}
	
	#recent-posts-intro li {
		display: flex;
		flex-direction: column;
		border: 1px solid var(--border);
		border-radius: 8px;
		overflow: hidden;
		transition: transform 0.2s, box-shadow 0.2s;
	}

	#recent-posts-intro li:hover {
		transform: translateY(-4px);
		box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
	}

	#recent-posts-intro a {
		text-decoration: none;
		color: inherit;
		display: flex;
		flex-direction: column;
		height: 100%;
		padding: 0;
	}

	#recent-posts-intro img {
		width: 100%;
		height: 200px;
		object-fit: cover;
		display: block;
	}

	#recent-posts-intro .title {
		font-size: 1.2rem;
		margin: 1rem 1rem 0.5rem 1rem;
		line-height: 1.4;
		flex-grow: 1;
	}

	#recent-posts-intro .date {
		font-size: 0.85rem;
		color: var(--muted);
		margin: 0 1rem 1rem 1rem;
	}

	@media (max-width: 768px) {
		#recent-posts-intro ul {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
	}

</style>

<BaseLayout>
	<body>
		<main>
			<h1>Hey there.</h1>
			<p>
				This is a space I would like to dedicate to my tech adventures, endeavours, deep dive into new domains and so on! <code>divyesh.is-cool.dev</code> will contain devlogs, writing about new discoveries and explorations into the fields of embedded hardware, and Linux development.
			</p>
			<p>
                I am currently focused on SleeQC, a TinyML framework, and optimizing neural networks for bare-metal environments. Visit the <code>about</code> page for my full journey and technical roadmap.
            </p>
			
		</main>
		
		<section id="recent-posts-intro">
			<h2>Recent Posts</h2>
				<ul>
					{ posts.length >0 ? posts.map((post) => (
							<li>
								<a href={`/blog/${post.id}/`}>
									{post.data.heroImage && (
										<Image width={720} height={360} src={post.data.heroImage} alt={post.data.title} loading={'eager'} />
									)}
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.pubDate} />
									</p>
								</a>
							</li>
						)) : 'No Posts Available.'
					}
				</ul>
			</section>
	</body>
</BaseLayout>
